# Library Management System

## Project Purpose

TODO: Write 3-5 sentences explaining the organization you chose and the problem
your program solves.

Example direction:

This project is a command-line library management system for a small community
library. It helps a librarian manage books, members, and borrowing records using
Python classes and CSV files.

## Features

TODO: Replace this list with the features you actually implement.

- Add and view books.
- Register and view members.
- Search for books.
- Borrow and return books.
- Save and load data using CSV files.

## Files

- `main.py`: runs the menu interface.
- `book.py`: defines the `Book` class.
- `member.py`: defines the `Member` class.
- `loan_record.py`: defines the `LoanRecord` class.
- `library.py`: defines the `Library` class.
- `data_handler.py`: handles CSV file reading and writing.
- `data/`: stores the CSV files used by the program.

## How to Run

TODO: Update this once your files are complete.

```bash
python main.py
```

## Example Usage

TODO: Add screenshots or copied terminal examples from your own testing.

```text
1. Add book
2. Register member
3. Search book
4. Borrow book
5. Return book
6. Exit
```

## Notes

This project was developed for learning Python programming concepts such as
classes, objects, methods, file handling, exception handling, and modular code.

